include ../config/config.pug
include ../components/**/*.pug

block variables

doctype html
html(lang=pageLang)
    head
        include ../parts/head.pug

    body.body
        .wrapper
            block header
                include ../parts/header.pug

            main.main
                block content

            block footer
                include ../parts/footer.pug

        block scripts
            block sdnScripts
            script(src="assets/js/main.min.js?v=" + siteVersion)
